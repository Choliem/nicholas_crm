<div class="mb-6">
  <h1 class="text-2xl font-bold text-slate-800">Daftar Pelanggan Aktif</h1>
  <p class="text-slate-500">Customer yang sudah berlangganan layanan.</p>
</div>

<div class="overflow-x-auto">
  <table class="min-w-full bg-white rounded-xl shadow-sm border border-slate-100">
    <thead>
      <tr class="bg-slate-50 text-slate-600 text-sm uppercase">
        <th class="py-4 px-6 text-left">Customer Name</th>
        <th class="py-4 px-6 text-left">Kontak</th>
        <th class="py-4 px-6 text-left">Layanan Berlangganan</th>
        <th class="py-4 px-6 text-right">Tanggal Bergabung</th>
      </tr>
    </thead>
    <tbody class="text-slate-700 divide-y divide-slate-100">
      <% @customers.each do |customer| %>
        <tr class="hover:bg-slate-50 transition">
          <td class="py-4 px-6">
            <div class="font-bold text-slate-800 text-lg"><%= customer.name %></div>
            <div class="text-xs text-slate-400 font-medium flex items-center gap-1">
              📍 <%= customer.address %>
            </div>
          </td>
          <td class="py-4 px-6">
            <div class="text-sm font-semibold"><%= customer.email %></div>
            <div class="text-xs text-slate-500"><%= customer.phone %></div>
          </td>
          <td class="py-4 px-6">
            <div class="flex flex-wrap gap-2">
              <% customer.projects.where(status: 'Approved').each do |project| %>
                <div class="flex flex-col gap-1 px-3 py-2 bg-blue-50 border border-blue-100 rounded-xl">
                  <span class="text-xs font-black text-blue-700 uppercase tracking-tight"><%= project.product.name %></span>
                  <span class="text-[10px] text-blue-400 font-mono"><%= project.svc_code %></span>
                </div>
              <% end %>
            </div>
          </td>
          <td class="py-4 px-6 text-right font-mono text-sm">
            <span class="text-slate-400 italic">Active</span>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
  <% if @customers.empty? %>
    <div class="text-center py-20 bg-slate-50 rounded-2xl border-2 border-dashed border-slate-200 m-6">
      <div class="text-4xl mb-4">🏠</div>
      <div class="text-slate-500 font-bold">Belum ada pelanggan aktif.</div>
      <p class="text-slate-400 text-sm">Project harus disetujui Manager untuk menjadi Pelanggan.</p>
    </div>
  <% end %>
</div>
