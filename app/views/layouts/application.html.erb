<!DOCTYPE html>
<html lang="id">
  <head>
    <title>PT SMART CRM | Integrated System</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <script src="https://cdn.tailwindcss.com"></script>
    
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    
    <style>
      body { font-family: 'Inter', sans-serif; }
      .glass-effect { background: rgba(255, 255, 255, 0.03); backdrop-filter: blur(10px); }
    </style>
  </head>

  <body class="bg-slate-50 text-slate-900 min-h-screen">

    <% if logged_in? %>
      <div class="flex">
        <aside class="w-64 bg-[#0A2463] text-white flex-shrink-0 fixed h-full shadow-2xl z-50 flex flex-col">
          <div class="p-6 border-b border-blue-800/50">
            <h1 class="text-xl font-bold tracking-tighter flex items-center gap-2">
              <span class="bg-red-500 w-3 h-3 rounded-full animate-pulse"></span>
              PT. SMART <span class="text-blue-400 font-light">CRM</span>
            </h1>
          </div>
          
          <nav class="mt-8 px-4 flex-1 space-y-2">
            <% [
              ['Dashboard', root_path, '🏠'],
              ['Leads (Prospek)', leads_path, '📈'],
              ['Projects', projects_path, '📁'],
              ['Master Produk', products_path, '📦']
            ].each do |name, path, icon| %>
              <%= link_to path, class: "flex items-center gap-3 py-3 px-4 rounded-xl transition-all duration-300 #{current_page?(path) ? 'bg-blue-600 shadow-lg translate-x-1 ring-1 ring-blue-400' : 'hover:bg-blue-800/50 opacity-70 hover:opacity-100'}" do %>
                <span class="text-lg"><%= icon %></span>
                <span class="font-semibold text-sm"><%= name %></span>
              <% end %>
            <% end %>
          </nav>

          <div class="p-4 border-t border-blue-800/50 bg-blue-900/20">
            <div class="px-4 mb-4">
              <span class="text-[10px] text-blue-400 uppercase font-black tracking-widest">Logged in as:</span>
              <p class="text-sm font-bold truncate"><%= current_user.name rescue 'User' %></p>
              <p class="text-[10px] bg-blue-700 inline-block px-2 py-0.5 rounded mt-1 text-white uppercase"><%= current_user.role rescue 'Staff' %></p>
            </div>
            <%= button_to logout_path, method: :delete, 
                class: "w-full flex items-center justify-center gap-2 py-3 px-4 text-red-400 hover:bg-red-950/30 rounded-xl transition-all border border-red-900/30 text-sm font-bold group",
                data: { confirm: "Apakah anda yakin ingin keluar?" } do %>
              <span>🚪</span> Keluar Sistem
            <% end %>
          </div>
        </aside>

        <main class="flex-1 ml-64 p-10">
          <% flash.each do |type, msg| %>
            <div class="max-w-4xl mx-auto mb-6 p-4 rounded-xl border flex items-center gap-3 shadow-sm animate-in fade-in slide-in-from-top-4 duration-500 
              <%= type == 'notice' ? 'bg-emerald-50 border-emerald-200 text-emerald-800' : 'bg-rose-50 border-rose-200 text-rose-800' %>">
              <span class="text-xl"><%= type == 'notice' ? '✅' : '⚠️' %></span>
              <p class="text-sm font-medium"><%= msg %></p>
            </div>
          <% end %>

          <div class="max-w-6xl mx-auto">
            <%= yield %>
          </div>
        </main>
      </div>

    <% else %>
      <nav class="bg-white border-b border-slate-200 py-4 px-8 flex justify-between items-center shadow-sm">
        <h1 class="text-xl font-black text-[#0A2463]">PT. SMART</h1>
        <div class="space-x-4">
          <%= link_to "Login", login_path, class: "text-sm font-bold text-slate-600 hover:text-blue-600" %>
          <%= link_to "Daftar Akun", signup_path, class: "text-sm font-bold bg-blue-600 text-white px-5 py-2 rounded-lg hover:bg-blue-700 transition" %>
        </div>
      </nav>

      <main class="w-full">
        <% flash.each do |type, msg| %>
          <div class="text-center p-4 <%= type == 'notice' ? 'bg-blue-100 text-blue-700' : 'bg-red-100 text-red-700' %>">
            <%= msg %>
          </div>
        <% end %>
        <%= yield %>
      </main>
    <% end %>

  </body>
</html>